apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: '{{ .deploymentName }}'
spec:
  interval: 5m0s
  path: ./_deploy/{{ .deploymentName }}/
  prune: true
  sourceRef:
    kind: GitRepository
    name: {{ .repositoryName }}